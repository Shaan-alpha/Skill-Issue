# v1.0.7 Low-Severity Hardening + Version-Fix — Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans. TDD each item; commit per item. Full detail per item is in the design spec `docs/superpowers/specs/2026-07-25-v1.0.7-low-severity-hardening-design.md`.

**Goal:** Fix the stale version display + clear eight small low-risk audit items. Ships as **v1.0.7**.

## Global Constraints
- No `Co-Authored-By:` trailers. Settings read at call-time where monkeypatched. No PII in log bodies.
- Backend gate: `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q`. Frontend: `cd frontend && npm run lint && npx tsc --noEmit && npm run test:run && npm run build`.
- Base `main` (has v1.0.6). Branch `fix/v1.0.7-low-severity-hardening` (created).
- **Version ritual bumps ALL THREE constants:** `backend/app/settings.py::VERSION`, `frontend/package.json::version`, `frontend/src/lib/site.ts::APP_VERSION`. **Do not tag/release.**

---

### Task 1 — VER: `APP_VERSION` bump + drift guard
- Create `frontend/src/lib/site.test.ts`: import `pkg from "../../package.json"`; assert `APP_VERSION === pkg.version`. Run → **fail** (`1.0.3 !== 1.0.6`).
- Set `frontend/src/lib/site.ts` `APP_VERSION = "1.0.7"` **and** `frontend/package.json` `"version": "1.0.7"`. Run → **pass**.
- `cd frontend && npx tsc --noEmit && npm run test:run -- src/lib/site.test.ts`.
- Commit: `fix(frontend): bump APP_VERSION + drift guard vs package.json (VER)`.

### Task 2 — ENV: secret repr scrub
- Append to `backend/tests/test_settings.py`:
```python
def test_secrets_not_in_repr(monkeypatch):
    monkeypatch.setenv("GITHUB_TOKEN", "ghp_SUPERSECRET")
    monkeypatch.setenv("OPENAI_API_KEY", "sk-SUPERSECRET")
    monkeypatch.setenv("SESSION_TOKEN_ENC_KEY", "A" * 44)
    r = repr(Settings())
    assert "ghp_SUPERSECRET" not in r
    assert "sk-SUPERSECRET" not in r
```
Run → **fail**.
- In `settings.py`, import `Field` (`from pydantic import Field`) and set `= Field(default=None, repr=False)` (or `Field(default="...", repr=False)` keeping current defaults) on: `github_token`, `openai_api_key`, `github_oauth_client_secret`, `session_token_enc_key`, `database_url`, `database_direct_url`, `upstash_redis_rest_token`, `cron_secret`, `revalidate_secret`, `internal_proxy_secret`, `sentry_dsn`. Run → **pass** (+ existing settings tests).
- Commit: `fix(settings): repr=False on secret fields so they don't leak into test output (ENV)`.

### Task 3 — SI-14: credentialed CORS wildcard guard
- Append to `test_settings.py`:
```python
def test_cors_wildcard_rejected():
    import pytest
    with pytest.raises(ValueError):
        Settings(cors_allow_origins="https://ok.com,*")
```
Run → **fail**.
- In `settings.py`, add a `@model_validator(mode="after")` (import `model_validator`) that raises `ValueError` if any comma-split entry of `cors_allow_origins` equals `*`. Run → **pass**.
- Commit: `fix(cors): refuse a credentialed wildcard origin at boot (SI-14)`.

### Task 4 — SI-35: README size cap
- Append to `tests/github/test_client.py`: a `get_repo_readme_text` mock returning an over-long base64 `content`; assert it returns without decoding the whole blob (result bounded / no error). Run → **fail**/adjust.
- In `github/client.py` `get_repo_readme_text` + `get_profile_readme`, add `content = content[:1_400_000]` before `base64.b64decode(content)`. Run → **pass**.
- Commit: `fix(github): cap README blob length before decode (SI-35)`.

### Task 5 — SI-36: commit-message truncation
- Append to `tests/scoring/test_depth.py`: a pathological long/pathological-scope message is scored without error and a normal conventional message still scores as before. Run → **fail**/adjust.
- In `scoring/depth.py` `_commit_message_quality`, truncate each `m` to 500 chars before `splitlines()`/regex; change `_CONVENTIONAL_PREFIX` scope group to `\((?:[^)]{0,100})\)`. Run → **pass**.
- Commit: `fix(scoring): bound commit-message regex on untrusted input (SI-36)`.

### Task 6 — SI-18: cron response trim
- In `tests/routers/test_cron.py` (confirm current assertions first), add/adjust: the 200 body has aggregate counts but **no** `owner_user_id` and no `target_login`. Run → **fail**.
- In `routers/cron.py` `refresh_saved_analyses`, drop the per-analysis `outcomes` array from the response body (keep `processed`/`succeeded`/`skipped`/`rate_limited`/`deadline_reached`/`purged_sessions`); if per-analysis visibility is wanted, `logger.info` it server-side (no PII beyond ids in the log, debug level). Run → **pass**.
- Commit: `fix(cron): drop owner/target ownership map from response body (SI-18)`.

### Task 7 — SI-34: encodeURIComponent
- In `frontend/src/app/u/[username]/page.tsx:42`, wrap: `/analyze/${encodeURIComponent(username)}`. Verify via `npx tsc --noEmit` + existing page tests (add a small assertion if a test exists for the fetch URL).
- Commit: `fix(frontend): encode username in the analyze proxy URL (SI-34)`.

### Task 8 — SI-33: SSE terminal-error signal
- Append to a frontend `sse.test.ts` (create if absent): mock `EventSource`; firing `onerror` invokes the `onError` callback (not `onComplete`). Run → **fail**.
- In `frontend/src/lib/sse.ts`, change `es.onerror` to call `onError(...)` (terminal — we already `es.close()`), not `onComplete()`. Confirm `narrative-stream.tsx` handles the error state (it has an error branch). Run → **pass** + `tsc`.
- Commit: `fix(frontend): SSE onerror signals failure, not completion (SI-33)`.

### Task 9 — Docs + version ritual (STOP before tag)
- Full gate (backend + frontend incl. build).
- Bump `settings.py::VERSION`, `frontend/package.json`, `site.ts::APP_VERSION` all to **1.0.7** (package.json + site.ts already done in Task 1 — confirm; bump `VERSION` here).
- `CHANGELOG [1.0.7]` (user-voice), `PLAN.md` (v1.0.7 shipped + remaining-backlog note), `PROGRESS_LOG` entry (incl. the APP_VERSION drift root cause + the now-enforcing test).
- Commit: `chore(release): prep v1.0.7 low-severity hardening (docs + version)`. **No tag.** PR → CI → pause.

## Self-Review
Covers all 8 spec items + the version ritual. Each task is TDD with a real failing test first (except SI-34 which is a 1-line consistency fix verified by tsc + existing tests). The `test_cron.py` assertion set must be read before Task 6 to avoid a false-green. `Field`/`model_validator` imports needed in settings.py (Tasks 2–3).
